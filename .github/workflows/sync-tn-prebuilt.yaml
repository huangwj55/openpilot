# 文件名: .github/workflows/sync-sunnypilot-tn-prebuilt.yml
# YAML - 严格使用 2 空格缩进，无 Tab

name: Sync sunnypilot tn-prebuilt with modifications (using GITHUB_TOKEN)

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout target repository (your repo baseline)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Add sunnypilot remote and fetch tn-prebuilt branch
        run: |
          git remote add sunnypilot_source https://github.com/sunnypilot/sunnypilot.git
          echo "Fetching tn-prebuilt from sunnypilot..."
          git fetch sunnypilot_source tn-prebuilt --depth=1 --no-tags
          echo "Fetch completed."

      - name: Checkout the fetched tn-prebuilt branch locally
        run: |
          echo "Checking out fetched branch into working directory..."
          git checkout -B tn-prebuilt refs/remotes/sunnypilot_source/tn-prebuilt
          echo "Working directory now contains tn-prebuilt content."

      # 关键改动：调用外部 Python 脚本
      - name: Apply custom modifications using external Python script
        run: python .github/scripts/apply_modifications.py # 直接运行脚本

      # 检查 Python 脚本执行后的文件状态
      - name: Check file status after modification script
        id: check_status
        run: |
          echo "Checking git status for modified files..."
          # 输出状态到 stdout，方便调试
          git status --porcelain system/athena/registration.py launch_openpilot.sh
          # 设置一个输出变量，表示是否有变更
          if [[ -n $(git status --porcelain system/athena/registration.py launch_openpilot.sh) ]]; then
            echo "changes_detected=true" >> $GITHUB_OUTPUT
          else
            echo "changes_detected=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit the changes
        # 仅当上一步检测到更改时才运行此步骤
        if: steps.check_status.outputs.changes_detected == 'true'
        run: |
          echo "Changes detected. Staging and committing..."
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add system/athena/registration.py launch_openpilot.sh
          # 再次检查暂存区，以防万一 add 后无实质更改 (理论上不会发生)
          if git diff --staged --quiet; then
             echo "Files were staged but content matches HEAD. Skipping commit."
          else
             git commit -m "Apply custom modifications via script after syncing from sunnypilot/tn-prebuilt"
             echo "Changes committed."
          fi

      - name: No changes to commit
        # 如果上一步未检测到更改，则打印信息
        if: steps.check_status.outputs.changes_detected == 'false'
        run: echo "No file changes detected by Python script. Nothing to commit."

      - name: Force push modified tn-prebuilt to your repository
        run: |
          echo "Pushing local tn-prebuilt to origin/tn-prebuilt..."
          git push origin tn-prebuilt --force
          echo "Push completed."
